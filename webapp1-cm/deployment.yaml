apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp1-cm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp1-cm
  template:
    metadata:
      labels:
        app: webapp1-cm
    spec:
      containers:
      - name: httpd
        image: httpd:2.4
        ports:
        - containerPort: 80

        volumeMounts:
        - name: html
          mountPath: /usr/local/apache2/htdocs/index.html
          subPath: index.html

      volumes:
      - name: html
        configMap:
          name: webapp1-cmhtml